const { useState, useEffect, useMemo, useCallback, useRef } = window.React || React;
const ReactDOM = window.ReactDOM;
const App = () => <div>Test</div>; ReactDOM.createRoot(document.createElement("div")).render(<App />);